# Rebuild boundaries and acceptance

Implemented a separate guest work surface to remove email/account barriers without weakening Supabase access controls. Guest operations never query or mutate clinic data. Legacy authentication remains separate and is not represented as repaired.

Marketing: three independent depth planes, factual scenario switch using the existing tested matching logic, progressive entrance motion, native scrolling, reduced-motion fallback. No invented testimonials, success rates, external photo assets or playback.

Guest journey: entry, matching, reviewed draft, copy, acknowledged manual/simulated send, response capture, medical/opt-out diversion, future booking record, close record, separate revenue confirmation. CSV and manual intake, unknown/missing information correction, duplicate protection, search and service/status filters, notes, snooze, dismissal, settings, backup and confirmed reset.

Test authority: unit tests and real browser scripts, exported app and production alias. Pixel-only local harnesses are design review, not network/auth acceptance. Screenshots at 1440x1000, 390x844, 360x640 and reduced-motion desktop. No claim of testing a physical iPhone.

No automatic WhatsApp, email, billing, invoice, calendar or CRM integration is advertised as connected. No cloud data copied into guest samples. Local backups contain the operator-provided data and must be handled as such.
